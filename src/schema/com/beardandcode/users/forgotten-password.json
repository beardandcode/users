{
    "$schema": "http://forms.beardandcode.com/draft-04/schema#",
    "title": "Forgotten password",
    "description": "If you have forgotten your password fill in your email address and a new password below.",
    "type": "object",
    "properties": {
        "email-address": {
            "title": "Email address",
            "description": "An email address",
            "type": "string",
            "format": "email"
        },
        "password": {
            "$ref": "#/definitions/password"
        },
        "repeat-password": {
            "$ref": "#/definitions/password"
        }
    },
    "definitions": {
        "password": {
            "type": "string",
            "format": "password"
        }
    },
    "order": ["email-address", "password", "repeat-password"],
    "submit": "Do a thing",
    "required": ["email-address", "password", "repeat-password"],
    "additionalProperties": false
}
