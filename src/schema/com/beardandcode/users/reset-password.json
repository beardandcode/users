{
    "$schema": "http://forms.beardandcode.com/draft-04/schema#",
    "title": "Reset your password",
    "type": "object",
    "properties": {
        "password": {
            "$ref": "#/definitions/password"
        },
        "repeat-password": {
            "$ref": "#/definitions/password"
        }
    },
    "definitions": {
        "password": {
            "type": "string",
            "format": "password"
        }
    },
    "order": ["password", "repeat-password"],
    "submit": "Reset your password",
    "required": ["password", "repeat-password"],
    "additionalProperties": false
}
